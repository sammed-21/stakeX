import React from "react";
import { StakedAmount } from "./StakedAmount";

type Props = {};

export const DisplayPannel = (props: Props) => {
  return (
    <div>
      <StakedAmount />
      staked
    </div>
  );
};
